import type { Device } from "../types/devices"

/**
 * Represents a PitchSix Force Board device
 * TODO: Add services, do you own a Force Board? Help us!
 */
export const ForceBoard: Device = {
  filters: [{ name: "Force Board" }],
  services: [],
}
