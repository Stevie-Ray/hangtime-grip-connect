import type { IDevice } from "../interfaces/device.interface"

/**
 * Represents a Climbro device
 * TODO: Add services, do you own a Climbro? Help us!
 */
export const Climbro: IDevice = {
  filters: [{ name: "Climbro" }],
  services: [],
}
