import type { IDevice } from "../interfaces/device.interface"

/**
 * Represents a mySmartBoard device
 * TODO: Add services, do you own a mySmartBoard? Help us!
 */
export const mySmartBoard: IDevice = {
  filters: [{ name: "mySmartBoard" }],
  services: [],
}
