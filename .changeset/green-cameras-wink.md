---
"@hangtime/cli": patch
---

Refactor CLI translations to a key-based `t(...)` system with dedicated locale files, add persistent language/unit
preferences, and localize interactive prompts and session status output across menus and stream actions.
